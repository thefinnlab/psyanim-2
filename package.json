{
  "name": "phaser_experiments",
  "version": "1.0.0",
  "description": "",
  "private": true,
  "main": "index.js",
  "scripts": {

    "watch": "rimraf -v ./dist && webpack --watch",
    "devwatch": "rimraf -v ./dist && webpack --config ./webpack.dev.config.js --watch",
    
    "editor": "rimraf -v ./dist && webpack && npx nodemon ./src/editor/main.mjs",
    "deveditor": "rimraf -v ./dist && webpack --config ./webpack.dev.config.js && npx nodemon ./src/editor/main.mjs",

    "build": "rimraf -v ./dist && webpack",
    "devbuild": "rimraf -v ./dist && webpack --config ./webpack.dev.config.js",

    "buildengine": "TODO: make this build the engine for jsPsych integration",

    "clean": "rimraf -v ./dist"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "express": "^4.18.2",
    "nodemon": "^2.0.22",
    "phaser": "^3.60.0",
    "rimraf": "^5.0.1",
    "ws": "^8.13.0"
  },
  "devDependencies": {
    "file-loader": "^6.2.0",
    "html-webpack-plugin": "^5.5.0",
    "webpack": "^5.79.0",
    "webpack-cli": "^5.0.1",
    "webpack-dev-server": "^4.13.2"
  }
}
