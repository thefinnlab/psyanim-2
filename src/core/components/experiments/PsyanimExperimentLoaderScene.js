import Phaser from 'phaser';

import PsyanimScene from '../../scene/PsyanimScene';

export default class PsyanimExperimentLoaderScene extends PsyanimScene {

    constructor() {

        super('_PsyanimExperimentLoader');

        // TODO: load scenes list from registry
        
    }

    create() {

        super.create();
    }

    update(t, dt) {

        super.update(t, dt);
    }
}